export function findNodeAtClick(x: number, y: number) {
  console.log("Finding node at click:", x, y);
  return null;
}

export function findNodesInSelectionBox({ x, y }: any, { x2, y2 }: any) {
  console.log("Finding node at click:", x, y);
  return null;
}
